--NodeMCU_SmartHome_ŒÂœn‚l„$Œ
--The comment above is mandatory for lua files uploaded through PUT requests!
--errorLedPin = 8

--gpio.mode(errorLedPin, gpio.OUTPUT)
--gpio.mode(greenPin, gpio.INPUT)
--gpio.mode(bluePin, gpio.INPUT)
--gpio.mode(redPin, gpio.INPUT)
--gpio.mode(1, gpio.OUTPUT)
--gpio.mode(2, gpio.OUTPUT)
--gpio.mode(3, gpio.OUTPUT)

--gpio.read(greenPin)
--gpio.read(bluePin)
--gpio.read(redPin)
